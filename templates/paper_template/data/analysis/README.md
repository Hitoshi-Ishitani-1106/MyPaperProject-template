# Analysis Code

Store analysis scripts used for manuscript Methods here (`.py`, `.R`, etc.).

At the top of each script, document I/O paths in this project layout:
- INPUTS: `data/processed/...`
- OUTPUTS: `data/tables/...`, `data/figures/...`

Workflow/QA helpers remain in `scripts/` and are not treated as analysis code for Methods.
