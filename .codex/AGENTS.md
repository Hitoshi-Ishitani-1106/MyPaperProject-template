# .codex AGENTS (Common Helper Rules)

- まず親 `AGENTS.md` を読み、次に対象論文の `papers/<paper_id>/AGENTS.md` を読む。
- 作業対象は原則 `papers/<paper_id>/` 配下に限定する。
- 変更は小さく行い、必要なら `make qa` で検証してから報告する。
- 規則化できる修正は `governance/preference_log.csv` に追記候補として残す。
